// Button enums for variants and sizes

/// Button variants available across the design system.
enum ButtonVariant { elevated, filled, outlined, text, tonal }

/// Button sizes available across the design system.
enum ButtonSize { small, medium, large }

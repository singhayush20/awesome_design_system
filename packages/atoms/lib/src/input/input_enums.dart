// Input enums for input types and sizes

/// Input types
enum DSInputType {
  text,
  email,
  password,
  number,
  phone,
  url,
  multiline,
  search,
}

/// Input sizes
enum DSInputSize { small, medium, large }

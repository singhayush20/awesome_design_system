// Card enums for variants and elevation levels

/// Card elevation levels
enum CardElevation { none, level1, level2, level3 }

/// Card visual variants
enum CardVariant { elevated, filled, outlined }
